/*
 * SPDX-FileCopyrightText: Copyright (c) 2024-2026, NVIDIA CORPORATION.
 * SPDX-License-Identifier: Apache-2.0
 */

#include <neighbors/iface/iface.hpp>

namespace {

using data_t  = @data_type@;
using index_t = @index_type@;

}  // namespace

namespace cuvs::neighbors {

using T_ha =
  raft::host_device_accessor<cuda::std::default_accessor<const data_t>, raft::memory_type::device>;
using T_da =
  raft::host_device_accessor<cuda::std::default_accessor<const data_t>, raft::memory_type::host>;
using IdxT_ha =
  raft::host_device_accessor<cuda::std::default_accessor<const index_t>, raft::memory_type::device>;
using IdxT_da =
  raft::host_device_accessor<cuda::std::default_accessor<const index_t>, raft::memory_type::host>;

template void build(
  const raft::resources& handle,
  cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  const cuvs::neighbors::index_params* index_params,
  raft::mdspan<const data_t, matrix_extent<int64_t>, row_major, T_ha> index_dataset);

template void build(
  const raft::resources& handle,
  cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  const cuvs::neighbors::index_params* index_params,
  raft::mdspan<const data_t, matrix_extent<int64_t>, row_major, T_da> index_dataset);

template void extend(
  const raft::resources& handle,
  cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  raft::mdspan<const data_t, matrix_extent<int64_t>, row_major, T_ha> new_vectors,
  std::optional<raft::mdspan<const index_t, vector_extent<int64_t>, layout_c_contiguous, IdxT_ha>>
    new_indices);

template void extend(
  const raft::resources& handle,
  cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  raft::mdspan<const data_t, matrix_extent<int64_t>, row_major, T_da> new_vectors,
  std::optional<raft::mdspan<const index_t, vector_extent<int64_t>, layout_c_contiguous, IdxT_da>>
    new_indices);

template void search(
  const raft::resources& handle,
  const cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  const cuvs::neighbors::search_params* search_params,
  raft::device_matrix_view<const data_t, int64_t, row_major> queries,
  raft::device_matrix_view<index_t, int64_t, row_major> neighbors,
  raft::device_matrix_view<float, int64_t, row_major> distances);

template void search(
  const raft::resources& handle,
  const cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  const cuvs::neighbors::search_params* search_params,
  raft::host_matrix_view<const data_t, int64_t, row_major> h_queries,
  raft::device_matrix_view<index_t, int64_t, row_major> d_neighbors,
  raft::device_matrix_view<float, int64_t, row_major> d_distances);

template void serialize(
  const raft::resources& handle,
  const cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  std::ostream& os);

template void deserialize(
  const raft::resources& handle,
  cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  std::istream& is);

template void deserialize(
  const raft::resources& handle,
  cuvs::neighbors::iface<ivf_flat::index<data_t, index_t>, data_t, index_t>& interface,
  const std::string& filename);

}  // namespace cuvs::neighbors
